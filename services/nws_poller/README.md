# nws-poller

Not yet implemented -- milestone 5 (`docs/design-spec.md` §10, §2).

Polls `api.weather.gov/alerts/active` with ETag-conditional requests.
`hybrid` profile only -- disabled entirely in `offgrid` mode (§8).
